const contacts = require("./contactsCtrl/index");
const users = require("./usersCtrl/index");
const current = require("./currentCtrl/index");
const avatar = require("./uploadAvatarCtrl/index");

module.exports = {
  contacts,
  users,
  current,
  avatar,
};
